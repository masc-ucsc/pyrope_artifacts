#DONE
a = :when c>300:
  return 3 #3 - cannot have a constant alone - need return statment 
end

b = :: # 3 - new scope must always begin in new line
  return 3  # Maybe better to have this for regularity
end

a = :when c<3: 
  return 3
end

if a; a=3
end

if a; a=3; b=2
end

#old syntax
#a.map(:: 3)
# .map(:: 3)
# .map(:: 3)

#new syntax
(a).map(:: a=3; end)
.map(:: a=3; end)
.map(:: a=3; end)
