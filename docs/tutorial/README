

# Export html

yarn install
./node_modules/backslide/bin/bs export

# Export PDF Docker option

docker run -t -v `pwd`:/home/user/slides -v ~:/home/user astefanutti/decktape /home/user/projs/pyrope/docs/tutorial/dist/presentation.html slides.pdf
docker cp `docker ps -lq`:slides/slides.pdf .

# Export PDF decktape (does not seem to run in arch)

./node_modules/decktape/decktape.js remark ./dist/presentation.html pp.pdf --no-sandbox

