#!/bin/bash

SCRIPT=`realpath $0`
export PRP_PATH=`dirname $SCRIPT`"/.."
if [ -f "$1" -o "--stdin" ]; then
  node $PRP_PATH/src/prp_parser_bin.js $1
  exit $?
else
  echo "ERROR: prp needs a valid input file as command line argument"
  exit 3
fi

